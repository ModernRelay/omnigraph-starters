# Second Brain — Reference Seed

A complete second-brain graph for **Alex Chen**, a fictional 36-year-old senior product designer living in Brooklyn with partner Maya and two kids (Noa, 5; Sam, 2). Alex works at Lumen Studio, is shipping a side project called Quiet Coach, training for a half-marathon, and trying to read more.

Everything in this seed is fabricated — names, places, and dates. The point is to give the schema realistic shape for demo queries.

**Totals:** 121 nodes across 12 types, 229 edges across 36 types. `Chunk` is declared in the schema but populated by a separate embedding pipeline, not in this seed.

`Knows` and `RelatedToPerson` edges are stored bidirectionally — if A knows B, the seed also loads B knows A; family edges store both `parent → child` and the inverse `child → parent`. This bidirectional storage is enforced by the loader — it's about creating the *inverse* edge, which `@unique` doesn't do. (Separately, `@unique(src, dst)` now enforces pair-uniqueness as a true composite key — intra-batch + at merge — though these edges don't declare it.)

## Layout

### People (16)
- **Self:** `per-self` (Alex Chen, designer)
- **Partner & kids:** `per-maya` (Maya Park, lawyer), `per-noa` (5), `per-sam` (2)
- **Alex's family:** `per-janet` (Mom, Vermont), `per-robert` (Dad, Florida), `per-hannah` (sister, Seattle)
- **Maya's family:** `per-david` (FIL), `per-elena` (MIL)
- **Close friends:** `per-theo` (since college), `per-priya` (work + friend), `per-marco` + `per-lia` (couple, kids' age)
- **Colleagues:** `per-jordan` (engineer at Lumen), `per-bea` (PM at Lumen)
- **Mentor:** `per-kenji` (former boss)

### Organizations (5)
- `org-lumen` (Lumen Studio — Alex's employer)
- `org-meridian-law` (Maya's law firm)
- `org-quietcoach` (Alex's side project as an LLC)
- `org-sunny-prek` (preschool)
- `org-anchor-pediatrics`

### Places (9)
Brooklyn (city), Park Slope home, Lumen office, Roman's (favorite Italian), Prospect Park, Sunny Lane Preschool, Burlington VT (Mom), Tampa FL (Dad), Seattle (Hannah)

### Areas (5)
Health, Career, Family, Learning, Creativity

### Goals (4)
Ship Quiet Coach beta (Q3), Run NYC Half (Oct), Read 24 books, Save $30k toward house

### Projects (6)
Quiet Coach (active), Kitchen renovation (active), Lumen Q2 client redesign (active), Half-marathon training (active), Sam's preschool decision (exploring), Summer Japan trip (exploring)

### Habits (5)
Morning pages, 3x weekly run, Sunday family dinner, Read before bed, Daily deep-work block

### Events (12)
Mix of past (Theo's birthday, Noa starting kindergarten, Maya's promotion, Hannah's visit, Lumen kickoff, Priya's baby shower) and upcoming (Quiet Coach demo day, Noa school conference, NYC half-marathon, Japan trip, Mom's birthday).

### Notes (21)
- **Principles (3):** default to yes for family, capture input separately from output, no-meetings Fridays
- **Preferences (5):** Theo loves Lagavulin, Priya is vegetarian, Mom prefers calls, Marco loves Sey coffee, Hannah loves film cameras
- **Journal (3):** dated reflections
- **Insights (3):** mid-30s energy curves, preschool tradeoff thinking, Maya cadence observation
- **Reflections (2):** Q1 2026, 8-year-anniversary with Maya
- **Questions (2):** should Quiet Coach pivot to B2B; which preschool for Sam
- **Decision (1):** committed to NYC Half
- **Idea (1):** AI coach onboarding flow
- **Quote (1):** Rilke

### Tasks (15)
A realistic GTD mix — `next`, `waiting`, `someday`, `inbox`. Includes:
- `tk-send-theo-book` (i-owe Theo a book rec)
- `tk-hannah-call-back` (they-owe — she said she'd call after vet appt)
- `tk-call-mom-sunday`, `tk-buy-mom-bday-gift`
- `tk-quietcoach-onboarding-flow`, `tk-research-prek-pivot`
- `tk-renew-passport`, `tk-pediatrician-sam`, `tk-confirm-japan-flights`
- `tk-respond-marco-text` (i-owe), `tk-followup-kenji-coffee` (i-owe mentor)
- `tk-update-resume` (someday)

### Media (12)
Books in finished/want/consuming, podcasts, one show, one paper, one documentary recommended by Maya.

### Artifacts (11)
Emails, WhatsApp messages, meeting notes, a voice memo, a calendar entry, a web clip, a journal capture — plus two derived artifacts (a doc v2 derived from v1, and an LLM summary of an email) wired through `DerivedFromArtifact` with `activity` enum.

### Chunks (0 in seed)
`Chunk` is declared in the schema with `embedding: Vector(3072) @embed("text")`. Embeddings are generated by a separate ingest pipeline against the loaded `Artifact.content` — not hand-loaded in the seed. After loading, semantic search becomes the killer "what was I thinking about X" query.

## Wow Queries (what this seed lights up)

| Alias | Input | Returns |
|---|---|---|
| `close-friends` | — | 4 close friends sorted by cadence; shows Theo, Priya, Marco, Lia |
| `person-recent-events` | `per-theo` | Theo's birthday dinner, half-marathon training event |
| `person-tasks-i-owe` | `per-theo` | "Send Theo book recommendation" task |
| `person-tasks-they-owe` | `per-hannah` | "Hannah said she'd call about vet" task waiting |
| `preferences-for-person` | `per-theo` | "Theo loves Lagavulin" preference note |
| `principles` | — | All 3 of Alex's operating principles |
| `open-questions` | — | "Should Quiet Coach pivot to B2B?", "Which preschool for Sam?" |
| `tasks-next` | — | The clarified GTD next-action list |
| `tasks-i-owe` | — | All open commitments across people |
| `area-projects` | `area-family` | Kitchen renovation, Sam's preschool decision, Japan trip |
| `area-projects` | `area-creativity` | Quiet Coach (also linked to `area-career`) |
| `project-tasks` | `proj-quietcoach` | Onboarding flow task + research task |
| `goal-tasks` | `goal-quietcoach-beta` | Tasks rolling up to the beta launch goal |
| `habits-active` | — | All 5 active habits with current streaks |
| `habit-principle` | `hab-morning-pages` | The principle that motivated it |
| `reading-queue` | — | Books with status=want |
| `media-recommended-by` | `med-jiro` | Maya as the recommender (alias takes a media slug, not a person slug) |
| `person-recommendations` | `per-kenji` | Books Kenji recommended |
| `events-recent` | — | Time-sorted recent events |
| `person-life-events` | `per-maya` | Maya's promotion as a life event |
| `artifact-derived-from` | `art-qc-design-doc-v2` | v1 doc this was derived from |

## Identity Convention

- `per-self` is the user. All other people are external.
- Slug prefixes are mandatory: `per-`, `org-`, `pl-`, `ev-`, `nt-`, `tk-`, `proj-`, `area-`, `goal-`, `hab-`, `med-`, `art-`.
