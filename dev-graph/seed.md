# Reference Seed — "Meridian" (fictional real-time-collaboration product)

Human-readable twin of `seed.jsonl`. All names, components, issues, and people
are fabricated. If you change one file, update the other (`seed.jsonl` is
generated by `.context/gen_seed.py`, which validates referential integrity).

**The product:** *Meridian* — a self-hostable real-time collaborative document
platform (CRDT-backed editor with live presence, offline sync, tenant isolation,
and usage billing). A five-person team plus a triage agent and a CI service actor.

**Totals (loaded):** **102 nodes across all 22 node types, 239 edges across 83
edge types.** The seed ships **no embeddings** (the `Vector(3072)` columns are
null — semantic search is opt-in; see the README). Load with `--mode overwrite`.

| Node type | # | Node type | # | Node type | # |
|---|---|---|---|---|---|
| Issue | 18 | Component | 8 | Actor | 7 |
| PR | 7 | Decision | 6 | Comment | 5 |
| Commit | 5 | SpecFile | 5 | Label | 5 |
| Capability | 4 | Invariant | 4 | Epic | 4 |
| Deployment | 4 | Release | 3 | Principle | 3 |
| Learning | 3 | Repo | 2 | Gap | 2 |
| ExternalBlocker | 2 | Assumption | 2 | Gate | 2 |
| Incident | 1 | | | | |

## Team (`Actor`)

| Slug | Name | Kind | Role (informal) |
|---|---|---|---|
| `act-nadia` | Nadia Okonkwo | human | Eng lead; owns `repo-meridian`, `epc-offline-sync` |
| `act-ravi` | Ravi Menon | human | Backend; owns sync-core, auth, storage, webhooks |
| `act-mei` | Mei Lin | human | Frontend; owns presence, web-client |
| `act-tomas` | Tomas Alvarez | human | SRE; owns `repo-infra`; incident commander |
| `act-priya` | Priya Shah | human | PM; owns tenant-isolation + billing epics |
| `agent:orion` | Orion | agent | Triage automation agent |
| `svc-ci` | CI Runner | service | Triggers staging/preview deploys, owns CI gate |

## Repos & Components

**Repos:** `repo-meridian` (the monorepo, owned by Nadia) · `repo-infra` (owned by Tomas).

**Components (all in `repo-meridian`):**

| Slug | Name | Criticality | Owner |
|---|---|---|---|
| `cmp-sync-core` | sync-core | critical | Ravi |
| `cmp-auth` | auth | critical | Ravi |
| `cmp-api-gateway` | api-gateway | high | Nadia |
| `cmp-web-client` | web-client | high | Mei |
| `cmp-storage` | storage | high | Ravi |
| `cmp-presence` | presence | medium | Mei |
| `cmp-billing` | billing | medium | Priya |
| `cmp-webhooks` | webhooks | low | Ravi (subcomponent of api-gateway) |

Dependencies (`ComponentDependsOn`): api-gateway → {auth, sync-core}; presence →
sync-core; web-client → api-gateway; sync-core → storage; billing → webhooks.

## Epics

| Slug | Title | Status | Owner | Target release |
|---|---|---|---|---|
| `epc-offline-sync` | Offline editing & sync | active | Nadia | v1.5.0 |
| `epc-tenant-isolation` | Hard tenant isolation | active | Priya | v1.4.0 |
| `epc-billing-v2` | Usage-based billing v2 | shaping | Priya | — |
| `epc-observability` | Sync observability baseline | done | Tomas | v1.3.0 |

## Issues (18)

Priority: 1=urgent … 4=low. Terminal states: done / canceled / duplicate.

| Slug | Title | Status | Pri | Type | Epic | Component |
|---|---|---|---|---|---|---|
| `iss-split-brain` | Split-brain writes under partition | done | 1 | bug | offline-sync | sync-core |
| `iss-tenant-check` | Enforce tenant check on storage reads | done | 1 | feature | tenant-isolation | storage |
| `iss-offline-queue` | Offline edit queue with replay | in_progress | 2 | feature | offline-sync | sync-core |
| `iss-offline-conflict-ui` | Conflict-resolution UI | todo | 2 | feature | offline-sync | web-client |
| `iss-presence-leak-fix` | Fix presence memory leak | done | 1 | bug | — | presence |
| `iss-presence-scale` | Rework presence fan-out | todo | 3 | task | — | presence |
| `iss-billing-metering` | Meter document-edit events | backlog | 3 | feature | billing-v2 | billing |
| `iss-billing-webhooks` | Stripe usage webhook handler | backlog | 3 | feature | billing-v2 | webhooks |
| `iss-tenant-migration` | Backfill tenant_id on legacy docs | todo | 2 | task | tenant-isolation | storage |
| `iss-auth-mfa` | Add TOTP MFA to auth | backlog | 2 | feature | — | auth |
| `iss-crdt-gc-guard` | Guard test for CRDT gc assumption | needs_review | 3 | chore | — | sync-core |
| `iss-flaky-sync-test` | Flaky convergence test in CI | todo | 2 | bug | — | sync-core |
| `iss-webhook-idempotency` | Idempotency keys for webhooks | done | 2 | feature | — | webhooks |
| `iss-observability-dash` | Grafana dashboards for sync | done | 3 | task | observability | api-gateway |
| `iss-rate-limit` | Rate-limit presence updates | hold | 3 | task | — | presence |
| `iss-safari-login` | Login fails on Safari | todo | 2 | bug | — | auth |
| `iss-dup-login-bug` | (duplicate of Safari login) | duplicate | 3 | bug | — | auth |
| `iss-offline-replay-test` | Property test for replay ordering | todo | 3 | task | offline-sync | sync-core |

**Dependency edges that make the derived queries interesting** — one per
blocking dimension, so every reason `ready()` excludes an issue has a matching
`blocked*` view (nothing is "neither ready nor blocked"):
- `iss-offline-queue` **blocks** `iss-offline-conflict-ui`; `iss-billing-metering`
  **blocks** `iss-billing-webhooks` (`IssueBlocksIssue`) → `blocked`.
- `iss-offline-queue` is **parent of** `iss-offline-replay-test`
  (`IssueParentChildIssue`) → `blocked-parent`.
- `epc-tenant-isolation` is **parent epic of** `iss-tenant-migration`
  (`EpicParentChildIssue`) → `blocked-epic`.
- `iss-tenant-migration` **waits for** `gate-security-review` (pending Gate,
  `IssueWaitsForGate`) → `blocked-gate`.
- `iss-offline-conflict-ui` **waits for** `iss-offline-replay-test`
  (`IssueWaitsForIssue`) → `blocked-wait`.
- `iss-offline-queue` **waits for** `blk-crdt-lib` (upstream, `IssueWaitsForBlocker`)
  → `blocked-upstream`.
- **`ready` returns 5:** presence-scale, billing-metering, auth-mfa,
  flaky-sync-test, safari-login (priority-ordered); **`blocked` returns 2.**

## Specs & Decisions

**SpecFiles (5):** `spc-offline-sync-rfc` (approved, specifies offline-sync) ·
`spc-tenant-isolation` (approved) · `spc-crdt-model` (approved, supersedes
`spc-crdt-model-v0`) · `spc-crdt-model-v0` (superseded) · `spc-billing-v2` (draft).

**Decisions (6, append-only):**

| Slug | Title | Status |
|---|---|---|
| `dec-crdt-v0` | Adopt a CRDT for concurrent editing | superseded |
| `dec-crdt-choice` | Adopt an RGA-based CRDT (supersedes v0) | accepted |
| `dec-tenant-boundary` | Enforce tenant isolation at storage | accepted |
| `dec-no-custom-wal` | Do not build a custom WAL | accepted |
| `dec-presence-backpressure` | Add backpressure to presence (from incident) | accepted |
| `dec-billing-provider` | Use Stripe metered billing (from `iss-billing-metering`) | proposed |

## Governance & substrate

**Invariants (4):** `inv-convergence` (holds) · `inv-tenant-isolation` (holds) ·
**`inv-single-writer` (violated)** · `inv-webhook-idempotent` (holds). →
`unheld-invariants` returns `inv-single-writer`.

**Principles (3):** `prn-respect-substrate` (prescribes) · `prn-no-custom-wal`
(forbids; exception granted to `pr-legacy-wal`) · `prn-no-string-sql` (forbids).

**Gaps (2):** `gap-single-writer` (widened; **undermines** `inv-single-writer`;
narrowed by `iss-split-brain` / `pr-partition-guard`) · `gap-presence-scale`
(narrowing; widened by `pr-presence-optimize`).

**External blockers (2):** `blk-crdt-lib` (automerge-rs, **open**, pinned 0.5.9,
nightly-gc workaround) · `blk-storage-engine` (SlateDB, **shipped** in 0.3.0).

**Assumptions (2):** `asm-crdt-gc` (guarded, covers `blk-crdt-lib`) ·
**`asm-storage-ordering`** (unguarded — no `guardRef`; a silent dependency).

**Capabilities (4):** `cap-realtime-cursors` (owned/shipped) · `cap-crdt-merge`
(inherited/shipped) · `cap-audit-log` (owned/shipped) · **`cap-offline-editing`**
(owned/**planned**, no implementing component → `no-cap-impl`).

## Code, release & operations

**Releases (3):** `rel-v1-3-0` (released) → `rel-v1-4-0` (released) →
`rel-v1-5-0` (cut; `artifactsChecklist` has one entry not yet `done`).

**Deployments (4, append-only):** v1.3 prod · v1.4 staging · v1.4 prod · v1.5 preview.

**PRs (7):** `pr-partition-guard` (merged) · `pr-tenant-check` (merged) ·
`pr-presence-optimize` (merged — the incident cause) · `pr-presence-hotfix`
(merged) · `pr-offline-queue` (open — waits on `blk-crdt-lib` + `gate-ci-green`) ·
`pr-billing-webhooks` (draft) · `pr-legacy-wal` (merged — the principle-exception).
5 `Commit`s link to their PRs.

**The incident storyline** (`inc-presence-leak`, S2, prod, resolved):
`pr-presence-optimize` cached peer lists without an eviction path → presence OOM
after the v1.4.0 prod deploy → `incident-cause` walks Incident → PR (`#205`) →
commit (`e5f6a7b`); blast radius is presence + api-gateway; `pr-presence-hotfix`
fixed it, `dec-presence-backpressure` emerged from it, and
`lrn-presence-postmortem` analyzes it.

**Learnings (3):** `lrn-presence-postmortem` (incident_review) ·
`lrn-crdt-gc-gotcha` (gotcha, about `blk-crdt-lib` + `asm-crdt-gc`) ·
`lrn-observability-retro` (retro, from `epc-observability`). *(A Learning's author
is its `createdBy` — there is no `LearningAuthoredBy` edge.)*

**Comments (5, append-only):** on `iss-split-brain`, `iss-offline-queue`,
`pr-presence-optimize`, `epc-offline-sync`, `inc-presence-leak`.

**Labels (5):** security · performance · crdt · tech-debt · good-first-issue.
**Gates (2):** `gate-ci-green` (passed) · `gate-security-review` (pending).
